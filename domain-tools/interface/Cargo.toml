[package]
name = "interface"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rref = { path = "../rref" }


[features]
default = ["blk","fs","uart","gpu","input"]
domain = []
blk = []
fs = []
uart = []
gpu = []
input = []