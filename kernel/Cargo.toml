[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "0"
ksync = { path = "../domain-tools/safety-abstraction/ksync" }
kcore = { path = "../base/kcore" }
platform = { path = "../base/platform" }
mem = { path = "../base/mem" }
arch = { path = "../base/arch" }
timer = { path = "../base/timer" }
riscv = "0"


rref = { path = "../domain-tools/rref" }
domain-helper = { path = "../base/domain-helper" }
domain-loader = { path = "../base/domain-loader" }
proxy = { path = "../base/proxy" }
interface = { path = "../domain-tools/interface" }
fdt = { path = "../safe-crates/fdt" }

[features]
default = ["smp"]
smp = ["platform/smp"]
gui = []