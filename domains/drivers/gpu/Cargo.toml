[package]
name = "gpu"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
interface = { path = "../../../domain-tools/interface", default-features = false, features = ["gpu","domain", "devices"] }
rref = { path = "../../../domain-tools/rref" }
basic = { path = "../../../domain-tools/basic" }
ksync = { path = "../../../domain-tools/safety-abstraction/ksync" }
virtio-drivers = { git = "https://github.com/rcore-os/virtio-drivers",rev = "de1c3b1"}
constants = { path = "../../../base/constants" }
spin = "0"