pub fn do_exit(_exit_code: i32) {
    // let task = take_current_task().unwrap();
    // task.exit(exit_code);
    // schedule_now(task);
    // todo!(remove task meta from scheduler_domain)
    // scheduler_domain!().remove_current_task();
}
